User: {
    login,
    password,
    profile: {
        bio: {
            name,
            photo,
            age
        }
        location,
        profession
    }
    createUser(login, password, {profileData})
    getUser(login)
}
